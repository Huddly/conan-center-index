[tool.poetry]
name = "docker-requirements"
package-mode = false # Only used for dependency management
version = "0.1.0"
description = ""
authors = ["Kristoffer Ellersgaard Koch <kristoffer@huddly.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">3.9,<4.0"
conan = ">=2.17.0 < 3"

[[tool.poetry.source]]
name = "PyPI"
priority = "primary"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
